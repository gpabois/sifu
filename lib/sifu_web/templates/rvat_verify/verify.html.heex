<div class="fr-container">
    <h1>RVAT N° <%= @process.id %> - Vérification</h1>

    <%= render SifuWeb.RVATIndexView, "resume.html", process: @process %>

    <%= form_for @changeset, Routes.rvat_verify_path(@conn, :execute, @task.id), [multipart: true, as: "verification"], fn f -> %>       
        <div class="fr-form-group   ">
            <fieldset class="fr-fieldset fr-fieldset--inline">
                <legend class="fr-fieldset__legend fr-text--regular" id='radio-verification-legend'>
                    Décision sur la vérification
                </legend>
                <div class="fr-fieldset__content">
                    <div class="fr-radio-group">
                        <%= radio_button f, :verified, true %>
                        <%= label f, :verified, "Accepter", class: "fr-label", for: "verification_verified_true"  %>
                    </div>
                    <div class="fr-radio-group">
                        <%= radio_button f, :verified, false %>
                        <%= label f, :verified, "Refuser", class: "fr-label", for: "verification_verified_false" %>
                    </div>
                </div>
            </fieldset>
            <%= error_tag f, :verified %>
        </div>

        <div class="fr-form-group fr-upload-group">
            <%= label f, :verify_document, "Document corrigé", class: "fr-label" %>
            <%= file_input f, :verify_document, class: "fr-upload" %>
            <%= error_tag f, :verify_document %>
        </div>

        <div class="fr-form-group"> <%= submit "Soumettre", class: "fr-btn" %> </div>
    <% end %>
</div>